<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Parks in Washington</title>
</head>
<body>
    <nav>
        <img src="/images/road.png" href="https://www.flaticon.com/free-icons/forest" title="forest">
        <a href="/">Parks in Washington</a>
        <form id="search-parks" action="/parks/search" method="GET">
            <input type="string" name="search-string">  
            <input type="submit" value="search">
        </form>
        <a href="/parks" <%- title === 'Parks' ? 'class="active"' : '' %>>Parks</a>
        <% if (user) { %>
        <a href="/parks/new" <%- title === 'Add park' ? 'class="active"' : '' %>>Add park</a>
        <img alt="avatar" src="<%= user.avatar %>" referrerpolicy="no-referrer" ><%= user.name %>
          <a href="/logout">Log out</a>
        <% } else { %>
          <a href="/auth/google" class="login">Log in&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
        <% } %>
    </nav>
<main>